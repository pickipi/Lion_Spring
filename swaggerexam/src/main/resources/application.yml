spring:
  application:
    name: swaggerexam

  datasource:
    url: jdbc:mysql://localhost:3307/swaggerdb
    username: like
    password: lion
    driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true
      properties:
        hibernate:
          format_sql: true
