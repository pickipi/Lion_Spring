spring:
  application:
    name: restexam

  servlet:
    multipart:
      max-file-size: 2MB
      max-request-size: 4MB

  datasource:
    url: jdbc:mysql://localhost:3307/jpadb
    username: like
    password: lion
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true