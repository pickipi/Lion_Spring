spring:
  application:
    name: securityexam

  datasource:
    url: jdbc:mysql://localhost:3307/jpadb
    username: like
    password: lion
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true